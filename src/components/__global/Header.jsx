import { Icon28LogoVkColor } from "@vkontakte/icons";

const Header = ({}) => {
  return (
    <div className="line">
      <Icon28LogoVkColor />
      <span style={{ fontWeight: 500 }}>ui</span>
    </div>
  );
};

export default Header;
